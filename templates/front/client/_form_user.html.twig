{% extends 'base.html.twig' %}
{% block body %}
<div class="col">
    {#<div class="card" style="width: 20rem;">#}
    <h2 class="titre1 container1">{{ action }} mes informations</h2>
    
    <div class="container1">
    <div class="card-5">
    {{ form_start(form) }}
        {{ form_errors(form) }}
        {{form_rest(form)}}
        <button type="submit" class="btn btn-success">{{ action }}</button>
    {{ form_end(form) }}
    </div>
    </div>
</div>
{% endblock %}
