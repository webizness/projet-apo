{% extends 'barman/base_barman.html.twig' %}

{% block body %}
<div class="container1">
<div class="card">
<div class="col ">

    <div style="width: 18rem;">
    <h2 class="titre5">{{ action }} mes informations</h2>

    {{ form_start(form) }}
    
        {{ form_row(form.nom) }}
        
        {{ form_errors(form) }}
     {{form_rest(form)}}   
     <button type="submit" class="btn btn-info">{{ action }}</button>
    {{ form_end(form) }}
        
</div>
</div>
</div>
</div>
{% endblock %}