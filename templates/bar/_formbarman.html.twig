{% extends 'bar/nav.html.twig' %}

{% block body %}
<div class="col container1">

   <div class="card">
    <h2>{{ action }} un barman</h2>
     
    {{ form_start(form) }}
    
        {{ form_row(form.nom) }}
        
        {{ form_errors(form) }}
        {{form_rest(form)}}
       <button type="submit" class="btn btn-success">{{ action }}</button>
    {{ form_end(form) }}
</div>
</div>
{% endblock %}